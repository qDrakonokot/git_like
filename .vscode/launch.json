{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug mygit",
            "type": "cppdbg",
            "request": "launch",
            // Путь к твоему скомпилированному исполняемому файлу
            "program": "${workspaceFolder}/mygit",
            // Аргументы, которые ты хочешь передать своей программе (например, "commit")
            "args": ["checkout", "master"], 
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            // Указываем VS Code автоматически запускать make перед отладкой
            "preLaunchTask": "make_build", 
            "miDebuggerPath": "/usr/bin/gdb" // или просто "gdb"
        }
    ]
}